export type BlogsType = {
    id: string
    name: string
    description: string
    websiteUrl: string
}

export type BlogsOutputType = {
    _id: string
    name: string
    description: string
    websiteUrl: string
}

